# Local development only, for `npm run fn:serve`.
# This file is gitignored as `supabase/functions/.env`. In production the key is
# set with `supabase secrets set OPENAI_API_KEY=...` and never lives on disk.
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini
